{
  "name": "server",
  "version": "1.0.0",
  "description": "ISS Tracker API",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "echo 'TODO: esbuild.. interview - no need'; exit 0 || node esbuild.config.mjs",
    "test": "echo 'TODO'; exit 0 || vitest",
    "lint": "eslint --max-warnings=0 .",
    "lint:fix": "npm run lint -- --fix"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "iss-schema": "../schema",
    "socket.io": "^4.8.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.5",
    "tsx": "^4.6.0",
    "vitest": "^0.34.6"
  }
}
